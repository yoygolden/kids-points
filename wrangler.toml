name = "kids-points"
pages_build_output_dir = "."
compatibility_date = "2024-11-01"

# D1 数据库绑定：函数里用 env.DB 访问
# 在 Cloudflare 控制台创建 D1 数据库后，把它的 ID 填到 database_id
# （dashboard 路径：Storage & Databases → D1 → 你的库 → 右侧复制 ID）
[[d1_databases]]
binding = "DB"
database_name = "kids-points-db"
database_id = "a1575cc5-f2bf-4f5d-acbd-4cc92d46bc0e"
