services:
  - type: web
    name: kids-points
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/health
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "18"
