# 小小积分银行 — GitHub 部署教程（账号云同步·跨设备）

> 目标：把整个项目（前端页面 + Node 后端）部署到一个免费 Node 平台，得到一个**固定网址**。
> 手机/平板/电脑用这个网址打开，注册/登录**同一个账号**，积分和记录就在所有设备间自动同步。
> 这与你之前的「运动打卡记录（SwimTrack）」用的是**完全相同的方式**：代码推到 GitHub → 平台从 GitHub 拉取运行。

---

## 一、原理（1 分钟看懂）

```
你手机 ──打开固定网址──▶ Railway / Render 上的 server.js
                           ├── 返回 kids-points.html（前端页面）
                           └── 处理 /api/*（账号注册/登录 + 整份数据同步）
```

- 后端 `server.js` **既托管网页又提供 `/api` 接口**（前后端同源）。
- 前端在「设置 → 账号云同步」里注册/登录后，每次保存都会把整份数据上传到云端；打开其他设备登录同一账号即自动下载。
- 数据默认写在 `./data/accounts.json`。**重点**：Railway/Render 的硬盘是临时性的，重启/重部署会清空——所以要在平台挂一块**持久磁盘**（见下文），云端账号数据才能真正永久保存。

---

## 二、准备：把代码推到 GitHub

> 本机代码已经 `git commit` 好了，只差推到远程。

1. 打开 https://github.com → 右上角 **New repository**
   - Repository name：`kids-points`（随意）
   - 选 **Public** 或 **Private** 都行
   - **不要**勾选 "Add a README"（保持空仓库）
   - 点 **Create repository**

2. 复制页面上给出的仓库地址，类似：
   ```
   https://github.com/你的用户名/kids-points.git
   ```

3. 在本机项目目录执行（把地址换成你自己的）：
   ```bash
   git remote add origin https://github.com/你的用户名/kids-points.git
   git branch -M main
   git push -u origin main
   ```
   推送成功后，GitHub 页面就能看到所有文件了。

---

## 三、方案 A：Railway 部署（推荐，支持永久磁盘）

Railway 免费额度对新项目较友好，且**支持挂载持久磁盘**，云端账号数据真正不会丢。

### 1. 注册并新建项目
- 打开 https://railway.app → 用 **GitHub 登录**
- 进入控制台 → **New Project** → **Deploy from GitHub repo**
- 选中刚才的 `kids-points` 仓库 → 确认部署

### 2. 等它部署完成
- Railway 会读 `railway.json`：构建跑 `npm install`，启动跑 `node server.js`
- 构建完，项目里会出现一个 **Web 服务**，状态变绿
- 点服务 → 顶部 **Settings → Domains → Generate Domain**
- 得到一个网址，类似：`https://kids-points-production.up.railway.app`
  👉 **这个网址就是你的 App 地址，收藏/发给家人即可。**

### 3.（关键）开启持久化磁盘，避免数据丢失
1. 在 Railway 项目里，点你的服务 → **Settings → Volumes → Create Volume**
   - Mount Path 填：`/data`
   - 大小 1 GB 足够（几乎免费）
2. 同服务 → **Variables** → 新增变量：
   - `DATA_DIR` = `/data`
   （`server.js` 会优先把账号数据写到这个目录）
3. 改完变量会自动重新部署一次。之后云端账号数据写在 `/data`，**重启也不会丢**。

> 不挂磁盘也能用，只是数据在重启后可能清空（靠各手机本地备份可恢复）。
> 建议家庭长期使用还是挂上这 1GB 磁盘。

### 4. 使用
- 手机浏览器打开上面的 Railway 网址
- 「设置」页 → **账号云同步** → 点「注册」一个账号（比如 `xiaoming` + 密码）
- 在另一台手机/电脑打开**同一个网址**，用同一账号**登录**
- 任意一端新增积分/奖励 → 底部状态显示「已同步」→ 另一端刷新即可看到
- 想放桌面快捷方式：手机浏览器菜单 →「添加到主屏幕」

---

## 四、方案 B：Render 部署（完全免费，但会休眠）

Render 免费档**不需要信用卡**，但：
- **15 分钟无访问会自动休眠**，下次打开要等几秒唤醒；
- **免费档没有持久磁盘**，休眠/重启后 `accounts.json` 会被清空。

适合"能接受偶尔重置、靠本地+导出备份"的轻量使用。

### 步骤
1. 打开 https://render.com → 用 **GitHub 登录**
2. 控制台 → **New + → Web Service**
3. 连接 `kids-points` 仓库
4. Render 会自动读取仓库里的 `render.yaml`，配置基本不用改：
   - Runtime：`Node`
   - Build Command：`npm install`
   - Start Command：`node server.js`
   - Plan：**Free**
5. 点 **Create Web Service**，等待部署
6. 部署完会给一个网址，类似：`https://kids-points.onrender.com`
   👉 这就是你的 App 地址。

### 使用注意（Render 免费档）
- 云端数据可能因休眠被清空 → 把**每台手机本地**当作主数据，**定期在「设置 → 数据备份」点"导出"**做备份。
- 云端清空后，任意一端点「上传本机」即可重新把数据推回云端。
- 如果以后想让数据永久保存，建议换到方案 A（Railway + 磁盘）。

---

## 五、账号云同步 怎么用（核心）

1. 打开部署好的固定网址（Railway/Render 域名）。
2. 进「设置」页，会看到 **👤 账号云同步（推荐·跨设备）** 面板（只有在后端可用时才显示；纯静态打开时不显示）。
3. 输入账号+密码，点「**注册**」：
   - 若本机已有数据 → 自动上传到云端作为初始数据；
   - 若云端已有该账号数据 → 会询问「用云端覆盖本机 / 把本机上传云端」。
4. 之后每次操作，状态栏显示「已同步 · 时间」即表示已上传。
5. 换设备：打开同一网址 → 同一账号「**登录**」→ 自动下载云端数据。
6. 「上传本机 / 下载云端」按钮可手动强制同步；「退出账号」清除本机登录态（云端数据不丢）。

> 三层保险：① 每台手机本地 `localStorage`；② 云端账号；③「数据备份」里的导出文件。

---

## 六、常见问题

**Q：部署后打开网页空白 / 显示 "云端服务暂不可用"？**
A：请确认你打开的是**部署平台给的网址**（Railway/Render 域名），而不是旧的 CloudStudio 链接。整套一起部署时前后端同域，不会出现这个提示。

**Q：账号云同步面板看不到？**
A：该面板仅在检测到后端（`/api/health` 可用）时显示。如果你打开的是纯静态链接（如 CloudStudio 旧链接或本地双击 html），它会被自动隐藏，此时可用下方的「家庭同步码」方式（旧版）。

**Q：忘记密码怎么办？**
A：目前后端没有"找回密码"。可在「数据备份」页「导出」备份，然后重新注册一个账号再「导入」。

**Q：数据存在哪里最安全？**
A：三层保险——(1) 每台手机本地 localStorage；(2) 云端账号（Railway 挂磁盘后永久）；(3) 手动"导出"下载的备份文件。清微信缓存/重装微信会清掉 (1)，所以 (2)(3) 很重要。

**Q：想换平台 / 删掉重来？**
A：直接在 Railway/Render 控制台删掉项目即可，不影响你手机本地的数据。
